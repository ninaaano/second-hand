import * as font from '@Styles/font';

import * as color from './color';
import * as mixin from './mixins';

//TODO: theme가 .했을때 자동으로 안 붙는 문제가 발생하여 추후 고쳐야함
export const theme = {
  color,
  font,
  mixin,
};
