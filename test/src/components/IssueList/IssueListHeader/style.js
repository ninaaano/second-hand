import styled from 'styled-components';

const $Nav = styled.nav`
  display: flex;
  align-items: center;
  gap: 16px;
`;

const $IssueListHeader = styled.header`
  display: flex;
  justify-content: space-between;
  align-items: center;
`;

export { $IssueListHeader, $Nav };
